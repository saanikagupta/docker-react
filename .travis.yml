language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t guptasaanika/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true guptasaanika/docker-react npm run test
